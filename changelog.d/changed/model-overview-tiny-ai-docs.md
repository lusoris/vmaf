- **docs(models)**: Refresh the human-facing model overview and FAQ so
  they describe the shipped `model/tiny/` ONNX registry instead of
  claiming tiny-AI weights are still future work. The docs now point
  operators to the registry, per-model cards, inference flow,
  signature verification, and smoke-vs-production row distinction.
