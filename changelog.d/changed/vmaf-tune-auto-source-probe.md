`vmaf-tune auto` non-smoke planning now uses a testable source probe seam
for ffprobe geometry, ffprobe duration, and HDR detection, and its CLI/docs no
longer describe the path as an unimplemented scaffold.
