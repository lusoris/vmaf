Replace stale `TODO` for `adm_p_norm` integration in `integer_adm.c` with a
structured `DEFERRED` block that documents why the exponent is hard-coded
(model-training coupling) and what the parameterised form would look like.
