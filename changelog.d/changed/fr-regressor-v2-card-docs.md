- **docs(ai)**: Refresh the `fr_regressor_v2` model cards so the main
  card describes the registry-promoted production checkpoint and the
  older `fr_regressor_v2_codec_aware` card is clearly marked as a
  superseded design note, not a still-planned ONNX artefact.
