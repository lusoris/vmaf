`vmaf-tune ladder --spacing vmaf` is now accepted by the CLI, and the
legacy `uniform` spelling remains supported as an alias for perceptual
VMAF-spacing instead of failing inside the ladder library.
