The DNN C API docs now describe the current execution-provider selectors
(CUDA, OpenVINO CPU/GPU/NPU, ROCm, CoreML variants) and the live `fp16_io`
behaviour instead of the retired CPU/CUDA-only status.
