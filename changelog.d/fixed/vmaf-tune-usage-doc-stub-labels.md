Retired stale `vmaf-tune` usage-doc stub labels for coarse-to-fine search,
Phase E ladder generation, the ladder default sampler, saliency-aware encode,
and fast recommend; the pages now describe the shipped implementations and
remaining production limits.
