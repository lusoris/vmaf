- **vmaf-tune**: Add Phase-B bisect sample-clip support and
  `vmaf-tune compare --sample-clip-seconds`, aligning FFmpeg `-ss` /
  `-t` with libvmaf `--frame_skip_ref` / `--frame_cnt` and bitrate
  normalisation.
