| [ADR-0363](0363-renovate-replaces-dependabot.md) | Mend Renovate replaces Dependabot as the dependency-update bot. Adds `renovate.json` (config:recommended base, weekly Monday schedule, grouped GitHub Actions minor+patch bumps, pre-commit hooks manager, Python patch grouping) and `.github/workflows/renovate.yml` (self-hosted via `renovatebot/github-action` SHA-pinned to v46.1.13 `79dc0ba74dc3de28db0a7aeb1d0b95d5bf5fde2a`). Disables `.github/dependabot.yml` (renamed to `.disabled` with a revert comment). Key addition over Dependabot: a `customManagers` regex rule tracking `FFMPEG_SHA:=n[0-9.]+` in `ffmpeg-patches/test/build-and-run.sh` and `FFMPEG_PATCHES_BRANCH` in `scripts/ci/ffmpeg-patches-check.sh` against `github-tags/FFmpeg/FFmpeg` — a surface Dependabot cannot reach. Operator doc: [`docs/development/dependency-bot.md`](../../docs/development/dependency-bot.md). | Accepted | ci, security, dependencies, github-actions, pre-commit, fork-local |
