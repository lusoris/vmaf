| [ADR-0193](0193-motion-v2-vulkan.md) | T7-23 / batch 3 part 1a — `integer_motion_v2_vulkan` extractor. Single-dispatch GLSL kernel that exploits convolution linearity (the SAD of blurred prev/cur frames equals the sum of absolute values of the blurred per-pixel diff), so each frame computes its score in one V→H separable convolve over `(prev_ref - cur_ref)`, accumulating absolute values into per-WG `int64` partials with no blurred-state buffer (vs `motion_vulkan`'s ping-pong). Raw-pixel ping-pong (`ref_buf[2]`) halves upload bandwidth vs framework `prev_ref`. **Bit-exact** vs CPU on both 8-bit (48 frames) and 10-bit (3 frames) — `max_abs_diff = 0.0` for both `motion_v2_sad_score` and `motion2_v2_score` on Intel Arc A380 + Mesa anv at 576×324; cross-backend gate runs at `places=4` for parity with the rest of the long-tail. Mirror padding **diverges** from `motion.comp` — CPU `integer_motion_v2.c` uses edge-replicating reflective mirror (`2*size - idx - 1`) while `integer_motion.c::edge_8`/`edge_16` use the non-replicating variant (`2*(size-1) - idx`); off-by-one at the boundary, GLSL must follow the CPU it's porting. CUDA + SYCL twins follow as ADR-0192 batch 3 parts 1b + 1c. | Accepted | vulkan, gpu, feature-extractor, fork-local, bit-exact |
