| [ADR-0323](0323-fr-regressor-v3-train-and-register.md) | `fr_regressor_v3` — train + register on `ENCODER_VOCAB` v3 (16-slot). Closes the v3 retrain deferral landed by ADR-0302 (PR #401). Adds [`ai/scripts/train_fr_regressor_v3.py`](../../ai/scripts/train_fr_regressor_v3.py) (mirrors the LOSO recipe from ADR-0319: 9-fold leave-one-source-out, fold-local StandardScaler, `FRRegressor(in_features=6, num_codecs=18)`, Adam(lr=5e-4, wd=1e-5), MSE, 200 epochs, batch=32) on the Phase A canonical-6 NVENC-only corpus (5,640 rows). Two-input ONNX `features:[N,6]` + `codec_block:[N,18]` → `vmaf:[N]` (16 ENCODER_VOCAB_V3 one-hot + preset_norm + crf_norm). **Gate PASS: mean LOSO PLCC = 0.9975 ± 0.0018**, every source above 0.99 — clears the ADR-0302 / ADR-0291 0.95 floor with ~5pp margin. Sidecar JSON mirrors `fr_regressor_v2.json` shape with `encoder_vocab_version: 3`, full per-fold trace, `corpus` + `corpus_sha256`. Registry row `fr_regressor_v3` lands `smoke: false`. **Live `ENCODER_VOCAB_VERSION = 2` in `train_fr_regressor_v2.py` stays authoritative** — v3 ships as a parallel checkpoint; v2 → v3 in-place promotion is a separate PR. NVENC-only corpus caveat documented honestly: 15 of 16 vocab slots receive zero training rows; ADR-0235 multi-codec lift floor (≥+0.005 PLCC) is not yet measurable on this corpus drop and is deferred to a multi-codec retrain. Companion model card [`docs/ai/models/fr_regressor_v3.md`](../ai/models/fr_regressor_v3.md). | Accepted | ai, fr-regressor, codec-aware, encoder-vocab, loso, fork-local |
