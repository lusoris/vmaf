| [ADR-0275](0275-vmaf-tiny-v3-v4-ptq.md) | T5-3d follow-up — adds dynamic-PTQ int8 sidecars for `vmaf_tiny_v3` and `vmaf_tiny_v4`, completing the VMAF feature-fusion ladder's quantisation coverage. Both produced via `ai/scripts/ptq_dynamic.py` (no calibration data required). v3: 4 496 B → 4 267 B (×0.95) with PLCC drop 0.000120 vs the 0.01 budget; v4: 14 046 B → 7 769 B (×0.55, -45 %) with PLCC drop 0.000145. v4 is the first MLP-flavoured tiny model where the on-disk size win is meaningful — v2/v3 graphs are dominated by op metadata + Constant scaler nodes rather than weights. Registry/sidecar gain `quant_mode: "dynamic"` + `int8_sha256` + `quant_accuracy_budget_plcc: 0.01`; the existing `ai-quant-accuracy` CI gate iterates over them transparently. Drops also stay inside budget on KoNViD (270k rows; 0.000177 / 0.000080). v2 stays fp32 — its 2 446-byte ONNX has too little weight mass for an int8 sidecar to deliver a useful size delta, and the quantiser's runtime overhead is unfavourable at that scale. | Accepted | tiny-ai, onnx, quantization, registry, fork-local |
