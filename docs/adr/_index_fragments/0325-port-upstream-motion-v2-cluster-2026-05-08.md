| [ADR-0325](0325-port-upstream-motion-v2-cluster-2026-05-08.md) | Port upstream motion_v2 four-commit cluster (`856d3835` mirror fix, `c17dd898` motion_max_val, `a2b59b77` motion_five_frame_window, `4e469601` remaining options + motion3). Manual port (cherry-pick conflicts on fork's clang-format-reformatted source). Mirror fix propagated to CPU + AVX2 + AVX-512 + NEON + CUDA + SYCL + Vulkan twins; new options are CPU-only. Adds `prev_prev_ref` end-to-end (feature_extractor.h, libvmaf.c, picture-pool sizing, vmaf.c CLI pic_cnt). Research-0089's claim that `c17dd898` was silently ported is corrected — the fork's `MotionV2State` had neither `motion_max_val` nor `feature_name_dict` before this PR. ffmpeg-patches replay: not impacted (no public C-API / `meson_options.txt` / `vf_libvmaf` surface touched). | Accepted | upstream-port, libvmaf, motion_v2, simd, cuda, sycl, vulkan, neon, ffmpeg-patches |
