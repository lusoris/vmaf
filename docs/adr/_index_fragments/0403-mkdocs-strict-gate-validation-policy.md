| [ADR-0403](0403-mkdocs-strict-gate-validation-policy.md) | mkdocs `--strict` validation policy. Tightens the existing `.github/workflows/docs.yml` `--strict` lane (which had been a smoke test because every link-validation category in `mkdocs.yml` was set to `info`): promotes `links.anchors` and `nav.{not_found,omitted_files}` to `warn` so the lane fails on broken in-doc anchors, mkdocs nav typos, and leaked excluded-tree pages; documents `links.{not_found,unrecognized_links}: info` carve-outs for the two unfixable populations (cross-tree pointers `../../libvmaf/src/...` outside `docs_dir`, and ADR-body cross-refs to renamed neighbours frozen by ADR-0028 / ADR-0106 immutability). Excludes `docs/adr/_index_fragments/**` from the rendered site (concatenation source per ADR-0221). Sweeps actionable subset: fixes two genuinely-broken anchors (`docs/mcp/embedded.md` → ADR-0209's "What lands next" heading, `docs/research/0055-...md` → Research-0053's "Distribution" heading) and the bare-relative-dir links in `docs/{index,state,rebase-notes}.md`. Net: strict-build flips from EXIT=1 with 1,276 emitted WARNINGs (after promoting categories to `warn`) to EXIT=0 with the actionable classes still gated. | Proposed | docs, ci, mkdocs, fork-local |
