| [ADR-0321](0321-fr-regressor-v2-ensemble-full-prod-flip.md) | `fr_regressor_v2_ensemble_v1` full production flip — real ONNX bytes + per-seed sidecars + `smoke: true → false`. Closes the ADR-0303 production-flip workflow. Adds [`ai/scripts/export_ensemble_v2_seeds.py`](../../ai/scripts/export_ensemble_v2_seeds.py) which reuses the LOSO trainer's `_load_corpus` for codec-block-layout fidelity, fits one `FRRegressor(in=6, hidden=64, depth=2, num_codecs=14)` per seed on the FULL Phase A canonical-6 corpus (5,640 rows; not LOSO held-out — that gate already passed per `runs/ensemble_v2_real/PROMOTE.json` mean PLCC 0.997, spread 0.001), exports five real-weight ONNX files (opset 17, two-input `features:[N,6]` + `codec_onehot:[N,14]` → `score:[N]`), writes per-seed sidecars `model/tiny/fr_regressor_v2_ensemble_v1_seed{N}.json` mirroring the canonical `fr_regressor_v2.json` shape (encoder vocab v2, codec_block_layout, scaler params, training_recipe) plus seed-specific gate evidence, and patches the registry sha256s + flips `smoke: false`. Closed predecessor PR #423 attempted a metadata-only flip; tripped `libvmaf/test/dnn/test_registry.sh` (no per-seed sidecars). The sidecar-generation half is what unblocks the gate. | Accepted | ai, fr-regressor, ensemble, prod-flip, registry, fork-local |
