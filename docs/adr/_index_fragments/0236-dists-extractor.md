| [ADR-0236](0236-dists-extractor.md) | DISTS [Ding 2020] tiny-AI extractor as the LPIPS companion flagged by Bristol VI-Lab's 2026 NVC review (Research-0033) §5.3 actionable #5. Adds `dists_sq` mirroring the existing [`lpips_sq`](0041-lpips-onnx-extractor.md) shape: VGG-16 backbone (`relu1_2`, `relu2_2`, `relu3_3`, `relu4_3`, `relu5_3` — same five layers DISTS specifies), two-input NCHW float32 (`ref` + `dist`, ImageNet-normalised — byte-identical to `lpips_sq.onnx` so `libvmaf/src/dnn/` host preprocessing reuses), scalar `score` per frame, registers via `VmafFeatureExtractor`-style table, picks up `model_path` from feature option or `VMAF_DISTS_SQ_MODEL_PATH` env (mirrors LPIPS / FastDVDnet). Decline contract `-EINVAL` if no model path. Smoke-only ship: ~330-byte randomly-initialised ONNX with the right I/O shape, marked `smoke: true` in `model/tiny/registry.json`. Real upstream-derived weights ([Ding's PyTorch reference](https://github.com/dingkeyan93/DISTS), MIT-licensed) tracked as **T7-DISTS-followup**. Op-allowlist: VGG-16 export hits `Conv`, `MaxPool`, `Relu`, `Flatten`/`GlobalAveragePool`, `Mul`, `Add`, `Div`, `Sub`, `ReduceMean` — already in the `lpips_sq` allowlist; no new op gating needed. Companion research digest: [Research-0033](../research/0033-bristol-nvc-review-2026.md). | Proposed | ai, fr, dnn, tiny-ai, fork-local, perceptual |
