| [ADR-0279](0279-vmaf-tune-codec-adapter-libaom.md) | `vmaf-tune` codec adapter — libaom-av1 (Google's reference AV1 encoder). Adds [`tools/vmaf-tune/src/vmaftune/codec_adapters/libaom.py`](../../tools/vmaf-tune/src/vmaftune/codec_adapters/libaom.py) wiring the canonical adapter contract plus `cpu_used(preset) -> int` mapping (`placebo=0 .. ultrafast=9`) and `ffmpeg_codec_args(preset, crf) -> tuple[str, ...]` returning the libaom-specific argv slice. Quality range `[0, 63]` (full libaom CRF window), default `35`. Preset vocabulary parallels x264/x265/svtav1 so a single sweep axis covers all four codecs without branching on codec name. Companion to the parallel x265 + SVT-AV1 adapter PRs; populates the fourth slot of the `fr_regressor_v2` six-slot codec one-hot (`x264, x265, svtav1, libaom, ?, ?`). Routing the codec-specific argv through `vmaftune.encode.build_ffmpeg_command` is a follow-up. | Accepted | tools, vmaf-tune, av1, codec-adapter |
