| [ADR-0272](0272-vulkan-vif-ciede-precise-decorations.md) | Vulkan vif + ciede shaders — `precise` (NoContraction) decorations on load-bearing FP ops (T-VK-1.4-BUMP step A). Audits `vif.comp` and `ciede.comp` and tags the FMA-contractable mul+add chains where the CPU reference computes scalar mul + add (no FMA): VIF's `g`, `sv_sq`, `gg_sigma_f` in the `integer_vif_scale2` output block; ciede's YUV→RGB / RGB→XYZ matmuls, `xyz_to_lab_map` linear branch, final Lab triple, `get_upcase_t` four-term cosine accumulator, `get_r_sub_t` chain, and the `ciede2000` per-pixel composition (62 NoContraction decorations in `vif.spv`, ~179 in `ciede.spv`). Decoration is a no-op at the current Vulkan 1.3 baseline — `places=4` cross-backend numbers are unchanged on RADV / lavapipe / Intel anv — but unblocks the future `VK_API_VERSION_1_3 → 1_4` bump (Step B, deferred) which under NVIDIA driver 595.x otherwise drifts `integer_vif_scale2` by 1.527e-02 (45/48 frames) and `ciede2000` by 1.67e-04 (42/48 frames). Verified on NVIDIA RTX 4090 / 595.71.05 (ciede 1.67e-04 → 8.9e-05) and RADV (unchanged at 8.3e-05). Companion research digest: [`docs/research/0062-vulkan-precise-decoration-audit.md`](../research/0062-vulkan-precise-decoration-audit.md). | Accepted | vulkan, fork-local, bit-exactness, docs |
