| [ADR-0235](0235-codec-aware-fr-regressor.md) | Codec-aware `fr_regressor_v2` — extends the v1 codec-blind FR MOS regressor with a one-hot codec input concatenated to the libvmaf feature vector before the first MLP layer. (a) Captures an explicit `codec` column in every per-clip parquet emitted by `ai/scripts/`; (b) extends `FRRegressor` with an optional `num_codecs` constructor arg (default `0` keeps the v1 single-input contract — existing checkpoints load unchanged); (c) ships a closed, order-stable codec vocabulary at `ai/src/vmaf_train/codec.py` (`x264`, `x265`, `libsvtav1`, `libvvenc`, `libvpx-vp9`, `unknown`). A `fr_regressor_v2_codec_aware` checkpoint registers in `model/tiny/registry.json` only after a side-by-side training run shows ≥ 0.005 PLCC lift on a held-out multi-codec split — otherwise the negative result is documented and the model does not ship. Bristol VI-Lab review §5.3 (`.workingdir2/preprints202604.0035.v1.pdf`) cites 1–3 PLCC/SROCC points typical lift on multi-codec corpora; Bampis 2018 (ST-VMAF) and Zhang 2021 (Bull lab "Enhancing VMAF") report similar deltas. Companion research digest: [Research-0040](../research/0040-codec-aware-fr-conditioning.md). | Proposed | ai, dnn, tiny-ai, fr-regressor, fork-local |
