| [ADR-0238](0238-vulkan-picture-preallocation.md) | Vulkan `VmafPicture` preallocation surface — closes the CUDA/SYCL/Vulkan API parity gap explicitly flagged in `docs/api/gpu.md`. Ships `VmafVulkanPicturePreallocationMethod` (`NONE` / `HOST` / `DEVICE`) plus `vmaf_vulkan_preallocate_pictures` + `vmaf_vulkan_picture_fetch` entry points, modelled on the SYCL surface (the SYCL pool is the simpler reference; CUDA's `HOST_PINNED` has no Vulkan analogue). `HOST` allocates via `vmaf_picture_alloc` (CPU-side score consumer); `DEVICE` backs each picture's luma plane with a host-visible Vulkan buffer (VMA `AUTO_PREFER_HOST`) and exposes the persistent mapped pointer via `pic->data[0]` so the caller writes once and the kernel descriptor sets read the same memory. New `VMAF_PICTURE_BUFFER_TYPE_VULKAN_DEVICE` tag lets cross-backend extractors refuse mixed-backing inputs. Pool depth fixed at the canonical `frames-in-flight = 2` (matches SYCL's compile-time constant); not part of the public surface in this PR — `ADR-0251`'s `max_outstanding_frames` knob is unrelated (controls the import-image fence ring, not the preallocation pool). Removes one host → device staging copy in FFmpeg-side host-driven integrations (the non-zero-copy path); existing import-image zero-copy path ([ADR-0186](0186-vulkan-image-import-impl.md) / [ADR-0251](0251-vulkan-async-pending-fence.md)) remains the right answer for external-VkImage callers. ABI grows two entry points + one enum + one struct (additive only). | Proposed | vulkan, api, preallocation, fork-local, parity |
