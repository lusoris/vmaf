| [ADR-0275](0275-vmaf-tiny-v3-registry-promotion.md) | Registry promotion for `vmaf_tiny_v3.onnx`. ADR-0241 accepted shipping v3 alongside v2 and the trained ONNX + sidecar + model card landed in-tree, but `model/tiny/registry.json` carried no `vmaf_tiny_v3` row — so the runtime tiny-model loader could not select it via `--tiny-model=vmaf_tiny_v3` and PR #383 (PTQ workstream) had to scope-shift back to v2. Adds a single registry row mirroring the v2 entry shape (kind `fr`, opset 17, BSD-3-Clause-Plus-Patent, sha256 `57b2b7e0…`, `smoke: false`); does not bump the production default (stays v2 per ADR-0241). Sidecar, model card, and ONNX file are unchanged. Unblocks PR #383 + future v3 follow-ups (multi-seed LOSO, KoNViD 5-fold, PTQ). | Accepted | ai, dnn, tiny-ai, model, registry, fork-local |
