| [ADR-0317](0317-ci-doc-only-pr-flake-fix.md) | Path-filter `Docker Image Build` and `FFmpeg Integration` (gcc / clang / SYCL / Vulkan lanes) so they fire only when their actual inputs change (`Dockerfile` + `libvmaf/**` + build-system for Docker; `libvmaf/**` + `ffmpeg-patches/**` + build-system for FFmpeg integration). Doc / Python / vmaf-tune-only PRs (#409, #411, #412) were burning 10–15 min of runner time per push: Docker tripped on a transient apt-mirror flake (exit 100) and FFmpeg-SYCL tripped on a real but pre-existing fork-local bug in patch `0008-add-libvmaf_tune-filter.patch` (`AVFilterLink::frame_rate` removed in FFmpeg n7+ — patches 0005/0006 already migrated to `ff_filter_link()`, patch 0008 did not). Neither workflow is in the `Required Checks Aggregator` (ADR-0313) allow-list, so the merge gate is unaffected; `workflow_dispatch:` preserved on both for manual triggers. Patch-0008 fix tracked as a follow-up — path-filter does not suppress it (any libvmaf/ or ffmpeg-patches/ touching PR will still surface it). | Proposed | ci, build, fork-local |
