# KoNViD-150k-A (K150K-A) — Feature Extraction

KoNViD-150k-A is the largest publicly available no-reference video quality corpus:
152,265 H.264 clips each annotated with a crowd-sourced mean-opinion-score (MOS).
This document covers how to extract the 22 FULL_FEATURES (Research-0026) from it
for tiny-AI model training.

## Quick start

```bash
# Smoke-test: 10 clips, ~70 s on RTX 4090
python ai/scripts/extract_k150k_features.py --limit 10

# Full run (background, ~296 h single-process)
nohup python ai/scripts/extract_k150k_features.py \
    > runs/k150k_extract.log 2>&1 &

echo "PID $!"
```

Both commands resume automatically from the `.done` checkpoint if interrupted.

## Options

| Flag | Default | Description |
|---|---|---|
| `--clips-dir` | `.workingdir2/konvid-150k/k150ka_extracted` | Directory of K150K-A `.mp4` clips |
| `--scores` | `.workingdir2/konvid-150k/k150ka_scores.csv` | CSV with `video_name`, `video_score` MOS labels |
| `--vmaf-bin` | `build-cpu/tools/vmaf` | Fork vmaf binary (must support ssimulacra2 + motion\_v2) |
| `--out` | `runs/full_features_k150k.parquet` | Output parquet path (gitignored) |
| `--threads` | `4` | vmaf `--threads` value |
| `--flush-every` | `1000` | Flush parquet every N clips |
| `--limit` | (none) | Process at most N clips (smoke-test mode) |
| `--no-cuda` | off | Disable CUDA backend and use CPU |
| `--scratch-dir` | `/tmp/k150k_yuv_scratch` | Temporary YUV decode directory |

## Output schema

The output parquet has one row per clip and 48 columns:

| Column(s) | Description |
|---|---|
| `clip_name` | Filename of the source `.mp4` |
| `mos` | Mean opinion score (1.0 – 5.0) |
| `width`, `height` | Decoded frame dimensions |
| `<feat>_mean` (22 cols) | Per-clip nanmean of the feature across frames |
| `<feat>_std` (22 cols) | Per-clip nanstd of the feature across frames |

Feature order follows `FEATURE_NAMES` in `ai/scripts/extract_k150k_features.py`
(column-order-locked — see `ai/AGENTS.md §K150K-A corpus extraction`).

## FR-from-NR adapter

K150K-A carries no reference video.  To run full-reference libvmaf extractors the
script uses the FR-from-NR adapter (ADR-0346): the decoded YUV is passed as both
`--reference` and `--distorted`.  This has two consequences:

- **Informative features** (cambi, motion, motion2, motion3, ssimulacra2) reflect
  actual content properties and are useful for training.
- **Constant features** (ADM, VIF, SSIM, VMAF) floor at their identity value;
  **null features** (ciede2000, psnr_hvs) are all-NaN.  Downstream training must
  drop or impute these columns.

## Hardware requirements

| Component | Minimum |
|---|---|
| GPU | NVIDIA with CUDA support |
| VRAM | 4 GB (540p clips) |
| CUDA driver | 520+ |
| Disk (scratch) | ~10 GB for temporary YUV files |
| Disk (output) | ~500 MB for the full parquet |

The system `/usr/local/bin/vmaf` v3.0.0 is **not** compatible — it lacks
`ssimulacra2` and `motion_v2`.  Use `build-cpu/tools/vmaf` (fork build):

```bash
meson setup build-cpu -Denable_cuda=true -Denable_sycl=false
ninja -C build-cpu
```

## Restartability

The script writes a `.done` checkpoint file (same path as `--out` with `.done`
extension) listing completed clip names, one per line.  On restart it skips
already-processed clips.  The parquet is written atomically via a `.tmp` sibling
every `--flush-every` clips and once more at the end.

## Further reading

- [ADR-0362](../../docs/adr/0362-k150k-corpus-integration.md) — design decision
- [Research-0067](../../docs/research/0067-k150k-corpus-integration.md) — feasibility digest and ETA analysis
- [ADR-0346](../../docs/adr/0346-fr-from-nr-adapter.md) — FR-from-NR adapter pattern
- [Research-0026](../../docs/research/0026-full-features-set.md) — FULL_FEATURES definition
