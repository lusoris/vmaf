matlab
workspace
python/.tox
.git
.venv
.venv*/
*.mp4
*.yuv
*.mov

# Local-only state dirs — without these the dev-MCP build context balloons
# to ~52 GB on dev hosts that hold the .workingdir2/ corpora (KonViD-150k
# 179 GB + CHUG 37 GB + scratch).
.workingdir2/
.workingdir/
.claude/

# Build trees
build*/
build-*/
libvmaf/build*/
libvmaf/build-*/

# Caches
__pycache__/
*.egg-info/
.tox/
.cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/
node_modules/

# Backup parquets / bench results that aren't source-of-truth
*.bak
*.bak.parquet
testdata/netflix_benchmark_results.json

# OS junk
.DS_Store
Thumbs.db
